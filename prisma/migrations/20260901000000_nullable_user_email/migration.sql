-- Телефонные пользователи больше не получают сгенерированный технический email
-- при регистрации (только при первом заказе, если он реально понадобится для
-- WooCommerce/RetailCRM) — поэтому email должен допускать NULL.
-- Unique-индекс на "email" уже существует и продолжает работать с NULL как обычно
-- в Postgres (несколько NULL не считаются дубликатами).
ALTER TABLE "user" ALTER COLUMN "email" DROP NOT NULL;
